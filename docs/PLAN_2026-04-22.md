# Plan — 2026-04-22 (after the late-night session)

Written ~2:30 AM after the arb-scraper session. Read this first thing when you pick back up.

## State you're walking into

- On branch `feat/arb-scraper-mvp`, pushed to GitHub. 9 commits tonight.
- 4 scrapers live and producing JSONL: Polymarket, Kalshi, NoVig, ProphetX.
- `/venues` page shipped with 37 venues, early-access email capture, Supabase migration 004 applied.
- `docs/HANDOFF_MARKETS_DASHBOARD.md` briefs the other Claude for the dashboard/terminal UI side.
- **Main domain (`sneakersterminal.com`) is NOT yet serving this work** — it tracks `feat/platform-scaffold` or `main`. Merge gate to decide first thing.

## Top priorities in order

### 0. **OG Markets capture FIRST** (priority override)

User-flagged 2026-04-22 late-night: OG Markets (og.com) is priority #1. It's the CFTC-regulated retail product on the Crypto.com Derivatives (CDNA) stack — also the backend for Underdog Predict. Scraping OG directly covers Underdog Predict's prices AND adds crypto-native contracts we don't have anywhere else in our pipeline.

Capture prompt already written at `docs/prompts/og-api-capture.md` — open it, hand to Claude Chrome, paste the structured report back, scraper follows in ~30 min.

Specifically flagged by user: crypto (BTC/ETH price contracts) is the key differentiator here.

### 1. Merge + deploy (15 min)

Decide whether to:
- **Option A:** merge `feat/arb-scraper-mvp` → `feat/platform-scaffold` → production. Puts `/venues` live on the main domain immediately. Risk: drags in 9 commits of scraper work on what was a platform-only branch.
- **Option B:** merge both `feat/arb-scraper-mvp` and `feat/platform-scaffold` → `main` and flip Vercel's production branch to `main`. Cleaner long-term but requires the branch-switch + verification.

Either way, get `/venues` reachable at `sneakersterminal.com/venues` before tester outreach begins.

### 2. Hand the dashboard/terminal UI to the other Claude (ongoing)

Already briefed in `docs/HANDOFF_MARKETS_DASHBOARD.md`. Their first task: a `/markets` (or `/dashboard/markets`) page that reads the JSONL files and renders market cards with cross-venue trade buttons.

Don't duplicate their work — stay on the data/scraper side.

### 3. State matrices — Phase 1 data model (30 min)

Extend `apps/platform/src/lib/venues.ts`:

```ts
interface Venue {
  // ...existing
  availableInStates: string[] | 'ALL_US' | 'VARIES';
  notableExclusions?: string[];
  stateDataVerifiedAt?: string;  // 'YYYY-MM-DD' so we know how stale
}
```

Wire a simple state-selector banner on `/venues`:
- Dropdown of US states, persisted in a cookie (`sneakers_state`)
- Cards show "Available in your state ✓" / "Not available in [STATE]" / "Availability unknown"
- Affiliate buttons hidden when not available; "Request early access" still works (signal of demand in that state)

Leave `availableInStates` empty/VARIES for all venues until Phase 2.

### 4. State data entry — Phase 2 (60–90 min)

Fill real state availability for all 37 venues. Best-effort public data from each operator's state-availability page. Set `stateDataVerifiedAt: '2026-04-22'`.

Categories with broadly similar footprints (shortcut):
- **Most regulated sportsbooks** (DK, FD, BetMGM, Caesars): ~20–26 states, usually the same overlap with minor variance
- **Prediction markets** (Kalshi, Polymarket, ProphetX, NoVig, DK Predictions): ~40–48 states; NY sued Coinbase/Gemini over this 2026-04-21 so NY is an active battleground; confirm current status
- **DFS pick'em** (PrizePicks, Underdog, Sleeper): ~30–35 states with different exclusion sets
- **Sweeps** (Thrillz, Fliff, etc.): ~48 states with WA/ID/MI/NV the common exclusions

Don't guess — check each operator's current state list before entering.

### 5. Sportsbook captures — at most 3 tonight's equivalent (1–2 hrs)

Use `docs/prompts/` pattern. Order by value:

1. **DraftKings Sportsbook** — widest vig, biggest arbs vs NoVig. Highest priority.
2. **FanDuel Sportsbook** — second independent sportsbook, validates DK.
3. **BetMGM** — third tier-1 for triangulation.

Flow per book:
- You: Chrome DevTools cURL capture (~15 min per book once flow is practiced)
- Me: build the scraper in `apps/trader/src/scrapers/<book>/scrape.ts` using the existing Kalshi/NoVig/ProphetX patterns. Reuse `utils/american-odds.ts` for the implied-prob conversion.
- Paste structured capture; scraper lives ~30 min later.

Stop after 3 sportsbooks tonight. The rest can follow over the week; there's no value in grinding 7 of them unless arbs are flowing visibly on the UI first.

### 6. Verify a real cross-book arb (30 min, high ROI)

By hand: pull up NoVig and Kalshi or ProphetX on the same NBA game. Find one where `ask_A + ask_B < 1.00` across books. Screenshot it.

This is your **launch artifact for the 100 testers**. "We're shipping an app that found a 2% arb on the Lakers last night" sells better than any marketing copy.

### 7. Game-level matcher (2–3 hrs)

Complement to `scanner/match-futures.ts`. New file: `apps/trader/src/scanner/match-games.ts`. Match key: `{sport, home_team, away_team, scheduled_date, market_type, line}` normalized across platforms. Output: `{game, platforms: [{name, ask, bid, affiliate_url}]}`.

Once this works, the dashboard's cross-book compare view becomes a direct render of its output.

### 8. 100-testers outreach (afternoon/evening)

Per `project_sneakers_100_testers.md`. Recruitment pitch + signup flow are still undefined. Admin page (in-flight per ROADMAP) is the operator side.

Practical questions to decide up front:
- Where do testers come from? (WINDAILY email list, Twitter, sports betting Discord servers, your existing socials?)
- What's the tester deal? (Free premium tier when we launch? Cash referral? Early access + credits?)
- What do you need from them? (Account connections per `project_sneakers_100_testers.md` — they supply session tokens for the no-public-API platforms via a guided flow)

## What's deferred / NOT doing tomorrow

- **ProphetX partner API application** — user explicitly said not now. Revisit when tokens start expiring painfully.
- **Timescale migration** — JSONL is fine for the week. Revisit when drift analysis becomes a real need.
- **Betr scraper** — mobile-only; requires mitmproxy setup. Revisit with fresh head on a dedicated day.
- **Coinbase Predict / Sleeper / Robinhood separate scrapers** — CONFIRMED Kalshi wrappers. Never scraping these separately. UI fans Kalshi data out to them as trade destinations.
- **Continuous polling loop** — scrapers stay one-shot until there's a specific product need (live drift chart, sub-minute arb alerts). Manual re-runs fine for now.

## Open questions worth resolving tomorrow

1. **Legal posture on scraping.** You're a WINDAILY affiliate partner with most books — that covers promoting them. It does NOT automatically cover capturing session tokens and replaying their private APIs. Before launching publicly, get a real answer on this. Low-risk for the prototype; pre-launch blocker.
2. **State matrix source of truth.** Manual data entry is fine for v1 but state laws change monthly. Longer-term: is there an authoritative feed (AGA? PlayUSA? LegalSportsReport?) we can ingest once a week rather than maintaining by hand?
3. **Affiliate signup status for Kalshi + Coinbase + Robinhood.** Per `project_wrapper_venues_as_trade_destinations.md` — you already have WINDAILY for Sleeper. Check Kalshi/Coinbase/Robinhood programs; sign up if not already.

## What to do first thing

1. Check git state (`git fetch && git status` — branches switch across your machines).
2. Decide the merge path (option A vs B above).
3. Send cURL capture for DraftKings Sportsbook. I'll have the scraper 30 min later.

This plan is the source of truth for the 2026-04-22 working session. Deviate as needed but update this doc (or ROADMAP.md) when priorities shift so tomorrow's tomorrow-you has context.
